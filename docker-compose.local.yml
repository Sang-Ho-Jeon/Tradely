# 로컬 테스트용: docker compose -f docker-compose.yml -f docker-compose.local.yml up --build
services:
  nginx:
    volumes:
      - ./nginx/default-local.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"

  # 로컬에서는 certbot 불필요
  certbot:
    profiles:
      - disabled
